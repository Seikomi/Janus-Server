package com.seikomi.janus.commands;

import com.seikomi.janus.net.JanusServer;

public class CommandsFactoryInitializer {

	public static void load(JanusServer server) {
		// TODO to clarify the adding of new user commands
		// CommandFactory commandFactory = CommandFactory.init(server);
		// commandFactory.addCommand("#TEST", new Test(server));
	}

}
